# Conventions

- Do not hardcode kiosk IDs, Manager credentials, tokens, or payment secrets.
- Persist Manager session with secure storage.
- Use `apply_patch` for source edits.
- Run `flutter analyze`, `flutter test`, and Windows build verification for release-impacting changes.
