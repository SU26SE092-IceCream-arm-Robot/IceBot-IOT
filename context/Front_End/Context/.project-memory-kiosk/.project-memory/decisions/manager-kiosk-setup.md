# Manager Kiosk Setup Decision

A Manager owns one store and has full access to every kiosk at that store. Login returns the Manager store scope. The client requests kiosks for that store, auto-selects a sole result, and presents an explicit kiosk selection UI for multiple results.
